#[derive(Debug)]
pub struct StructureParticle {
    pub time: f64,
    pub rip_strength: f64,
    pub scale_factor: f64,
    pub position_x: f64,
    pub position_y: f64,
    pub position_z: f64,
    pub velocity_x: f64,
    pub velocity_y: f64,
    pub velocity_z: f64,
    pub run_id: i64,
}
